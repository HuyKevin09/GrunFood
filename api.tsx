//import React from 'react';
//import {auth, firebase, database} from './firebase'

/*
export const SignInUser = (email, motDePasse) => {

  return new Promise(function(resolve, reject){
    auth
      .signInWithEmailAndPassword(email, motDePasse)
      .then(() =>{
        resolve('Connexion réussie');
      })
      .catch(error => {
        reject(error)
      });
  });
};
*/